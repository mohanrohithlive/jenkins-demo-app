{
  "name": "jenkins-demo-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "jest-junit-reporter": "^1.1.0",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "0.9.5"
  },
  "devDependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test:watch": "react-scripts test --env=jsdom",
    "test": "CI=true react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "test:ci": "CI=true react-scripts test --env=jsdom --testResultsProcessor `pwd`/node_modules/jest-junit-reporter"
  }
}
